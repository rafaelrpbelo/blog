<%= notice %>

<h2><%= @post.title %></h2>

<% @post.tags.each do |tag| %>
    <small><%= tag %></small>
<% end %>


<p><%= @post.created_at %></p>

<p><%= @post.body %></p>



<%= link_to t('link.action.back'), posts_path %>
